{
"scripts": {
  "install-client": "cd Client && npm install && npm run build && cd ..",
  "install-admin": "cd Admin && npm install && npm run build && cd .. ",
  "install-server": "cd Server && npm install && node server.js",
  "heroku-postbuild": "npm run install-client && npm run install-admin"
}
}
